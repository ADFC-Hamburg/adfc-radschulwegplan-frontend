import { LeafletPmtoolbarDirective } from './leaflet-pmtoolbar.directive';

describe('LeafletPmtoolbarDirective', () => {
  it('should create an instance', () => {
    const directive = new LeafletPmtoolbarDirective();
    expect(directive).toBeTruthy();
  });
});
